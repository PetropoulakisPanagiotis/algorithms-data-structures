#ifndef TYPE_LIST_1 // Compile once.
#define TYPE_LIST_1
#include <stdio.h>
#include <stdlib.h>
typedef int TElementList; // Type of elements in list.

void Set_Value(TElementList *x_ptr, TElementList y);

#endif
